// ignore_for_file: constant_identifier_names

class RemoteConstants {
  static const BASE_URL = 'https://reqres.in';
  static const SEND_TIMEOUT = Duration(seconds: 10);
  static const RECEIVE_TIMEOUT = Duration(seconds: 10);
  static const CONNECTION_TIMEOUT = Duration(seconds: 10);
}
